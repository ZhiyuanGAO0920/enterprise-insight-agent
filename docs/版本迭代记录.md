# Enterprise Insight Agent V4 — 版本更新迭代记录

> 依据 git 提交历史（59 条，2026-06-02 ～ 2026-07-31）整理，按版本阶段分组。
> 当前状态：V4.6.0（报告分享功能），192 条测试（190 通过 / 2 失败——LLM API 连接环境问题）。

---

## 一、版本总览

| 版本 | 时间 | 定位 | 核心变化 |
|------|------|------|----------|
| V1.0 | 2026-06-02 | 单 Agent MVP | 初始功能集 |
| V4 | 2026-06-12 | Multi-Agent 完整版 | 10 Agent 编排、流式报告、RBAC、Demo 数据 |
| V4 稳定期 | 06-15 ~ 07-07 | 工程化补全 | License、部署文档、n8n 模板、Redis 降级 |
| V4 评估期 | 07-23 | 作品集/面试准备 | 六维综合评估报告 + 面试攻防手册 |
| V4.5 | 2026-07-29 | 前端重构 | 全新 UI、反馈闭环、布局改造、监控页 |
| V4.5 修复潮 | 07-30 | 稳定加固 | 安全加固、性能优化、布局/体验修复 |
| V4.5 冲刺 | 07-31 | 性能优化 | Redis 缓存、simple 查询加速、回归修复 |
| V4.6 | 2026-07-31 | 报告分享 | 分享链接（30 天有效）、长图导出、PDF 降级修复 |

---

## 二、详细迭代记录

### V1.0（2026-06-02）— 起点

- `c0d1e0c9` Enterprise Insight Agent v1.0，单 Agent 初步实现

### V4（2026-06-12）— Multi-Agent 架构成型

- `0a1f18e9` **V4: Full feature set** — 看板、引导页、SSE 流式分析、RBAC 修复、Demo 数据（100 门店 / 50,925 订单 / 5,000 会员 / 30 供应商）

### V4 稳定期（06-15 ~ 07-07）— 工程化与运维

- `87b0ff79` 添加 LICENSE、性能指标体系、Prompt 变更日志
- `bd897730` 移除 GitHub Actions workflow（后续再加）
- `00a6b378` 修复 n8n webhook 配置、n8n 模板、部署文档、.gitignore
- `25c19846` 修复并行 Agent 进度闪烁、V2 登录兼容、Demo 录制、作品集 gitignore
- `d393e19d` **Redis 宕机优雅降级**（影响主流程不影响分析）

### V4 评估期（2026-07-23）— 综合评估报告（8 个文档提交）

- `192640e8` 评估报告设计规格
- `ebc4c46c` 评估报告实施计划
- `8bf3974c` 评估报告 HTML 骨架与全局样式
- `c37148c2` 执行摘要 + 六维雷达图
- `4b523d0b` PM 视角评估（决策链审视 + AI 边界 + 场景覆盖 + 方法论）
- `c087239a` 开发师视角评估（架构 + 代码审计 + 安全 + 测试 + 文档一致性）
- `843c30e2` 面试攻防手册（四维度攻防策略）
- `67473234` 最终润色 + 全量验证

### V4.5（2026-07-29）— 前端重构里程碑

- `048a2d62` **V4.5: 前端重构 + 反馈闭环 + 布局改造**（⚠️ 此提交引入信任分级、deleteUser/resetPassword/impersonateUser 丢失，已由 317b85aa 修复）
- `6a47fb5e` SSE 超时保护 + 追问修复 + 数据时效指示器 + KPI 动画 + 首次引导
- `91c7c2bb` V4.5 changelog（产品功能 + 安全修复 + 设计优化）
- `f665262f` 更新截图与作品集匹配 V4.5 全新 UI
- `8577c336` 移除登录页测试账号信息（安全）
- `6e1ff3ee` 质量监控仅管理员可见：侧边栏 + API 双控
- `28ce2e13` 修复编辑用户门店选择器缺失 + store_id 类型处理
- `d84731d1` 修复 doAddUser store_id 类型（parseInt → string）

### V4.5 修复潮（2026-07-30）— 26 条提交集中加固

**体验与弹窗（09:09 ~ 09:41）**
- `2b76a696` 编辑用户弹窗布局：分区标题 + 双列网格 + 门店复选框网格
- `2dfad787` 过期 token 自动检测：401 时清除 token 并跳转登录页
- `8c05d4ed` API 错误提示 `[object Object]` → 显示具体验证错误消息
- `f5f52af7` 新增用户错误提示：弹窗内中文消息，不自动关闭

**进入系统闪现修复 5 连发（09:58 ~ 10:17）**
- `10dd5fdb` app.js 先验证 token 再恢复会话
- `6f6dcc5e` 同步 XHR 验证 token + 兜底定时器防闪屏
- `1341a030` restoreSession 先验证 token 再显示界面
- `07665f94` head 内联脚本优先清除过期 token（渲染前）
- `18e7af93` 服务端禁止缓存 HTML + 内联脚本提前清除过期 token

**登录加载提速（10:35 ~ 11:09）**
- `f22a0230` restoreSession 先显示界面再异步加载角色信息
- `2dd8b0b1` 轻量级 `/auth/verify` 端点替代 `/admin/users` 验证 token
- `b3150517` 移除多余内联加载脚本，消除额外网络往返

**代码质量与性能（11:36 ~ 11:52）**
- `d14c9b03` code review 修复：全局替换损坏（Math.random/Array.from）、过期 token 跳登录、多余 checkAdmin
- `38d312b4` **看板 SQL 并行化**（asyncio.gather 减少 50% 等待时间）⚠️ 此提交丢失 recent_orders_24h 字段，已由 317b85aa 修复

**文档与布局（12:24 ~ 12:52）**
- `e9620c93` 文档版本号与测试计数更新（V4.5, 177 tests）
- `2e306a8f` PRD 文档版本号 v4.0 → v4.5
- `3323b8a8` 系统管理表格操作列换行修复：action-link nowrap
- `ebe1735a` 系统管理表格布局：数据范围列 text-overflow 截断
- `67deaaed` 质量监控错误列表列间距修复
- `9848fe97` 错误列表表头对齐：加 icon 空列

**监控与中文化（13:00 ~ 13:09）**
- `d60cf665` 错误信息中文化（timeout→超时、connection refused→拒绝等）
- `94827000` 修复质量监控崩溃：_errCn 变量提升导致 undefined 引用

**Demo 优化与回滚（13:25 ~ 13:35）**
- `49c1aaff` 面试 Demo 优化：函数名写全 + 死代码清理 + 作品集故事线
- `0679dc19` 修复 ts()→toggleScopeFields 替换误伤 renderEmptyStats()
- `856a63aa` / `5251b09c` 两次 Revert（误伤修复与 Demo 优化回滚）

**权限与安全（13:59 ~ 15:28）**
- `8bedbf45` 修复非管理员看板数据为空：store_ids 字符串转 int，适配 PostgreSQL ANY
- `9097079d` bump views.js v4.39→v4.40 强制浏览器刷新缓存
- `88e041d5` 看板加载失败静默降级：_cachedFetch 返回 null 而非 rejected promise
- `419928f5` **前端安全加固**：DOMPurify XSS 防护、事件委托、ECharts 泄漏修复

**收尾（15:36 ~ 16:07）**
- `3824f48d` Demo 视频脚本 v4：同步能力卡片、看板缓存、安全加固
- `8c4f0091` 修复质量监控标签：分析总量→日均分析量

### V4.5 性能冲刺（2026-07-31）— 响应提速

- `5130e2d6` 修复欢迎页卡在"正在加载"：token 验证竞态条件 + 超时兜底
- `5455f839` **流式分析结果写入 Redis 缓存，重复问题秒回**
- `11f1fae4` **simple 查询跳过图表推荐和质检，省 2 次 LLM 调用（~15-20s）** ⚠️ 引入 report_agent 路由缺失回归，已由 317b85aa 修复
- `317b85aa` **回归修复 8 项**：
  - simple 查询 KeyError 崩溃（条件边映射漏配 report_agent）
  - 看板「近24小时订单」字段恢复（并行化重构丢失）
  - 报告信任分级恢复（V4.5 重构丢失，3 处渲染入口补回）
  - deleteUser / resetPassword / impersonateUser 恢复（管理功能静默失效）
  - bat 文件 GBK 乱码修复（重启服务 / deploy / 每日投喂）
  - 测试环境隔离（REDIS_URL 受 .env 干扰）

### V4.6.0（2026-07-31）— 报告分享

- **分享链接**：`POST /analysis/share` 生成 30 天有效链接，`GET /analysis/share/{token}` 免登录只读渲染报告 + 图表；迁移 011（`analysis_history` 新增 `share_token`/`share_expires_at` 列 + 索引）
- **长图导出**：报告按钮条新增「🔗 分享」「🖼️ 长图」；分享优先调起系统分享（Web Share API，恢复 V4.5 重构丢失的 `navigator.share`），降级打开链接弹窗；长图用 html2canvas 导出 PNG（本地库 1.4.1，零 CDN 惯例）
- **PDF 修复**：weasyprint 缺系统库时降级 reportlab 纯 Python 渲染（自动探测微软雅黑/宋体/Noto CJK + ToUnicode 映射）；修复 PDF 中文文件名 `Content-Disposition` 500（RFC 5987 `filename*`）
- **复制按钮补回**：流式报告路径缺失「📋 复制」按钮（V4.5 重构遗漏，现两处一致）
- **新增测试**：`tests/phase7/test_report_share.py` 6 条（生成 / 免登录查看 / 失效 / 取消 / 跨用户 404）
- ✅ 本阶段改动已全部入库：分享功能主体（`e1a74256`，07-31）+ 遗留改动全量（`ede5b55d`，08-03）

---

## 三、当前状态（2026-08-03）

- **版本**：V4.6.0（报告分享功能）
- **测试**：192 条（190 通过 / 2 失败——LLM API 连接环境问题）
- **前端**：views.js v4.50（DOMPurify 安全防护 + 事件委托 + 分享/长图）
- **性能**：看板 SQL 并行化（-50% 等待）、流式结果 Redis 缓存（重复问题秒回）、simple 查询省 2 次 LLM 调用
- **已知待办**：无已知功能缺陷（V4.6 改动已全部入库：`e1a74256` + `ede5b55d`）

---

## 四、迭代统计

| 阶段 | 提交数 | 主题 |
|------|--------|------|
| V1.0 | 1 | 起点 |
| V4 | 6 | Multi-Agent + 工程化 |
| V4 评估期 | 8 | 评估报告与面试资料 |
| V4.5 | 9 | 前端重构 |
| V4.5 修复潮 | 26 | 稳定加固 |
| V4.5 冲刺 | 4 | 性能优化 |
| V4.6（未提交） | — | 报告分享、长图导出、PDF 降级 |
| **合计** | **59** | 2026-06-02 ～ 2026-07-31（另含 V4.6 未提交改动） |
